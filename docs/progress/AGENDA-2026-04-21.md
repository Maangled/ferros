# FERROS Agenda — 2026-04-21

Companion plan to [`PROGRESS.md`](./PROGRESS.md). Captures the detailed agenda following Wave 0 closure (2026-04-17, re-verified 2026-04-18) and Reconciliation Gate closure (2026-04-19). Ordered by dependency. Each item has a binary definition of done so the gate discipline from `PROGRESS.md` holds.

## Snapshot at authoring time

- **Wave 0 (C1–C10):** ✅ CLOSED
- **Reconciliation Gate (Wave 1 entry):** ✅ CLOSED (issue #53)
- **Wave 1 (V1–V8):** 8 of 9 ✅ — V4 (alias → claim → XP merge) remains ⬜
- **Wave 0 harnesses:** H1–H7 + Preflight green; **H8 needs clean rerun**
- **Consumer helpers** (`FerrosCore.loadProfile/saveProfile/pushAuditEntry`): artifact ✅, **enforcement gap**
- **Wave 2 (S1–S4):** S3 ✅; S1, S2, S4 ⬜

## Phase A — Foundation Finalization

Goal: close every residual gap on Wave 0 + Wave 1 so Wave 2 starts without retrofits.

### A1. Consumer-helper harness
- **Scope:** New harness exercising `FerrosCore.loadProfile()`, `FerrosCore.saveProfile()`, `FerrosCore.pushAuditEntry()` against the full fixture corpus plus corruption/quota edge cases.
- **DoD:** Harness green in Chrome via `file://`; `PROGRESS.md` "Known gaps" note removed; reconciliation-gate non-blocking hardening note resolved.
- **Why first:** Only foundation integrity hole. Every downstream surface (Arena export, Builder Lab, command center) calls these helpers.

### A2. H8 clean rerun + sign-off
- **Scope:** Fresh browser session; document environmental requirements that produced "not stable enough" state; capture N/N result in `PROGRESS.md`.
- **DoD:** H8 row flips from 🔧 to PASS with pinned count; instability root-caused in a short note.

### A3. V7 evidence note
- **Scope:** Append round-trip evidence for a real Forge-authored card into `CLOSURE-EVIDENCE.md` (or a Wave 1 evidence doc).
- **DoD:** V7's ✅ has a linked, reproducible artifact — not just a checkmark.

### A4. V4 — Alias → export → claim → XP merge
- **Scope:** The remaining Wave 1 vertical-slice capability. Journey 2 end-to-end.
- **DoD:** V4 flips to ✅; H5 acceptance harness exercises the alias-claim path end-to-end.

### A5. Audit findings #9 and #19 reconcile
- **Scope:** Re-evaluate finding #9 (card/deck in export) against V5–V7 closure — resolve or ticket. Re-evaluate finding #19 (fixture co-location) — keep deferred or schedule.
- **DoD:** Both rows updated in the remediation table with explicit disposition.

**Wave 1 exit after Phase A:** V1–V8 all ✅, every harness green, zero non-blocking hardening notes.

## Phase B — Wave 2 Entry (deliberate expansion)

Goal: cross the "contracts actually get consumed by >1 surface" threshold.

### B1. Arena Export Target ADR
- **Scope:** ADR defining exactly what a Forge-exported deck looks like when consumed by Arena Runtime (C8). Decide the shipping crate before building the loading dock.
- **DoD:** ADR accepted; referenced by both `forge.md` and `arena-runtime.md`.
- **Why before S1/S2:** Arena export is the smallest cross-surface flow. Lock it before Schedule Ledger or Battle Arena start consuming.

### B2. C6 runtime consumption spec
- **Scope:** Promote `FerrosCore.templateToEvents()` from transformation bridge to consumed contract. Write the consumer contract S1 will target.
- **DoD:** Schedule-event runtime consumption documented in `storage-rules.md` or a new contract doc; no longer a "Known gap."

### B3. S1 — Schedule Ledger first consumer
- **Scope:** Per `docs/progress/schedule-ledger.md`; reads profile/template data via shared contract only.
- **DoD:** S1 ✅; dedicated harness (or extended H1) validates Schedule Ledger ↔ C2/C6 consumption.

### B4. S2 — Battle Arena consumer
- **Scope:** Consume Arena Runtime without custom data paths (per `docs/progress/trading-arena.md`).
- **DoD:** S2 ✅; demonstrates two surfaces on the same runtime host.

### B5. S4 — Contract freeze at v1
- **Scope:** Once ≥3 surfaces consume shared contracts, freeze v1 and document the migration policy for v2.
- **DoD:** S4 ✅; schema `version` semantics documented; Wave 4 H1 (schema migration) now has a real v1 to migrate from.

**Wave 2 exit:** S1–S4 ✅, ≥3 surfaces consuming shared contracts, contracts frozen.

## Phase C — Parallel Lanes (kick off anytime during B)

Non-blocking, but benefit from early start so they are ready when needed.

- **C1. Legacy L1** — harness drift detection (extend `generate-harness-constants.ps1`). Wave 0 target. Mechanical win.
- **C2. Legacy L4** — three-layer decomposition standard; accept ADR-014 on first Rust code. Needed when Research track touches product.
- **C3. Research R1** — renderer conformance suite skeleton, using the same fixture corpus as web surfaces. Sets up Wave 4 H2 cross-platform validation.

## Phase D — Wave 3 Prep (stage, do not start)

Prerequisites to keep in sight so Phase B decisions do not accidentally corner Wave 3.

- **Agent trait spec for C8 (L2):** Any Arena-export ADR decision in B1 must not foreclose agent-driven updates.
- **Command bus for audit enforcement (L3):** Design the `pushAuditEntry()` harness (A1) so a command bus can wrap it without refactor.

## Tracking

Phase A items are tracked under a single umbrella issue: **"Phase A — Foundation Finalization."** Phase B items will be filed as individual issues at Phase A exit. Phase C items may be filed opportunistically. Phase D is tracked here as a watch-list only.