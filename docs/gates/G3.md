# Gate G3 — Agent Center on Runtime Demo

**Status:** ⬜ Blocked on G2  
**Streams:** S3 + S4 (joint gate)  
**Unlocks:** S5 Phase B (local web shell); S7 implementation

---

## Entry criteria

- G2 is closed (profile v0 frozen; `ProfileId` and `CapabilityGrant` types available).
- S3 and S4 implementation work is underway.

---

## Required evidence

The following must all be true and verifiable before G3 closes:

**S4 Runtime:**
- [ ] `crates/ferros-core/` builds and passes `cargo test`.
- [ ] `crates/ferros-runtime/` builds and passes `cargo test`.
- [ ] Policy engine: 10+ unit tests covering grant/deny scenarios.
- [ ] Property tests for policy engine (e.g., via `proptest` or `quickcheck`).
- [ ] `crates/ferros-core/` compiles with `--no-default-features` (validates `no_std` readiness).

**S3 Agent Center:**
- [ ] `crates/ferros-agents/` builds and passes `cargo test`.
- [ ] `echo` reference agent: registers, receives grant, spawns, echoes, stops.
- [ ] `timer` reference agent: registers, spawns, fires periodic events, stops.
- [ ] `ferros agent list | describe | run | stop | logs` all functional.

**Joint (S3 + S4 integration):**
- [ ] `ferros-node demo` runs deterministically: starts runtime, registers both reference agents, verifies deny-by-default, exits 0.
- [ ] Deny-by-default verified: ungranted capability request from either reference agent is blocked and logged.
- [ ] The `echo` and `timer` agents use real `CapabilityGrant` types from S2 (no stubs).

---

## Exit criteria

All required evidence items checked. A PR with these items reviewed and merged. `ferros-node demo` runs green in CI.

---

## Blocking streams / issues

G3 must close before:

| Stream | Blocked item |
|--------|-------------|
| S5 Phase B | Local web shell talks to `ferros-agents` over JSON/RPC; S3 JSON/RPC API must exist |
| S7 Hub | `ferros-hub` wraps `ferros-node`; runtime must be stable |

---

## Notes

G3 is the "FERROS is real" moment internally. After G3, the system has a consent-enforced capability bus, a working agent registry, two reference agents, and a CLI. This is the MVP threshold — `v0.1.0-rc` should be tagged when G3 closes.
