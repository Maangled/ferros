# Gate G3 — Agent Center on Runtime Demo

**Status:** 🟡 Active  
**Streams:** S3 + S4 (joint gate)  
**Unlocks:** S5 Phase B (local web shell); S7 implementation

---

## Entry criteria

- G2 is closed (profile v0 frozen; `ProfileId` and `CapabilityGrant` types available).
- S3 and S4 implementation work is underway.

---

## Required evidence

The following must all be true and verifiable before G3 closes:

**S4 Runtime:**
- [x] `crates/ferros-core/` builds and passes `cargo test`.
- [x] `crates/ferros-runtime/` builds and passes `cargo test`.
- [x] Policy engine: 10+ unit tests covering grant/deny scenarios.
- [x] Property tests for policy engine (via `proptest`).
- [x] `crates/ferros-core/` compiles with `--no-default-features` (validates the landed pre-`no_std` compile slice).

**S3 Agent Center:**
- [x] `crates/ferros-agents/` builds and passes `cargo test`.
- [x] `echo` reference agent: registers, declares capability requirements in its manifest, spawns, echoes, stops.
- [x] `timer` reference agent: registers, spawns, fires periodic events, stops.
- [x] `ferros agent list | describe | run | stop | logs` all functional.

**Joint (S3 + S4 integration):**
- [x] `cargo run --bin ferros -- demo` runs deterministically through the current `ferros` binary: starts runtime, registers both reference agents, verifies the current deny-by-default path, exits 0.
- [ ] CI is now wired to run both `cargo check -p ferros-core --no-default-features` and `cargo run --bin ferros -- demo` on `ubuntu-latest`; a recorded green hosted run reference is still pending.
- [x] Deny-by-default verified for the current demo path: an ungranted capability request is blocked and logged.
- [x] The current runtime/demo path authorizes `echo` and `timer` starts using the real S2 `CapabilityGrant` contract (no stubs).

---

## Exit criteria

G2 is closed. All required evidence items remain checked. A PR with these items is reviewed and merged.

---

## Blocking streams / issues

G3 must close before:

| Stream | Blocked item |
|--------|-------------|
| S5 Phase B | Local web shell talks to `ferros-agents` over JSON/RPC; S3 JSON/RPC API must exist |
| S7 Hub | `ferros-hub` wraps `ferros-node`; runtime must be stable |

---

## Notes

G3 is the "FERROS is real" moment internally. After G3, the system has a consent-enforced capability bus, a working agent registry, two reference agents, and a CLI with the remaining gate evidence behind it. This is the MVP threshold — `v0.1.0-rc` should be tagged when G3 closes.

G2 is now closed, so G3 is active. The current repo already has the convergence path in place: `cargo run --bin ferros -- demo` exercises the real current `CapabilityGrant` type through the `ferros` binary, CI is now wired to run that demo and `cargo check -p ferros-core --no-default-features`, the local `ferros agent ...` CLI is in tree and test-backed, and `ferros-core` now has both policy property tests and the `cargo check -p ferros-core --no-default-features` proof. G3 now remains open because a recorded green hosted CI run reference is still pending for those workflow steps. JSON/RPC, reusable host hardening, and the S5 shell remain post-G3 work.
