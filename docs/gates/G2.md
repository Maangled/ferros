# Gate G2 — Profile v0 Frozen

**Status:** 🟡 Active  
**Stream:** S2  
**Unlocks:** S3 active development; S7 pairing design

---

## Entry criteria

- G1 is closed (CI is green; Cargo workspace exists).
- S2 implementation work is underway.

---

## Required evidence

The following must all be true and verifiable before G2 closes:

- [x] `crates/ferros-profile/` builds and passes `cargo test`.
- [x] `ProfileId` type is defined and public.
- [x] `CapabilityGrant` type is defined, signed, and verifiable.
- [x] `ConsentManifest` type is defined.
- [x] Profile round-trip works end-to-end: create → serialize → sign → verify → revoke.
- [ ] `schemas/profile.v0.json` JSON Schema is present and frozen (no further mutations).
- [x] `schemas/capability-grant.v0.json` JSON Schema is present and frozen.
- [x] Dedicated frozen profile golden fixture evidence is landed. `schemas/fixtures/profile-valid.json` now exists and is proven against the current `schemas/profile.v0.json` freeze candidate in `ferros-profile` and the H1 contract validator.
- [x] At least one golden fixture in `schemas/fixtures/grant-valid.json`.
- [x] At least one negative fixture: invalid signature is rejected.
- [ ] CLI: `ferros profile init | show | export | import | grant | revoke` all functional.
- [x] `ferros profile init` followed by `ferros profile show` succeeds on Linux.

---

## Exit criteria

All required evidence items checked. A PR with these items reviewed and merged. Schema version `profile.v0.json` and `capability-grant.v0.json` marked frozen — no further in-place mutations; new fields require a `v1` schema.

---

## Blocking streams / issues

G2 must close before:

| Stream | Blocked item |
|--------|-------------|
| S3 Agent Center | `ProfileId` and `CapabilityGrant` types required for agent manifest and consent check |
| S7 Hub | Pairing flow uses the profile API; cannot design pairing without stable types |

---

## Notes

G2 is now the active gate. G1 is closed, so the remaining work here is implementation and freeze evidence rather than an upstream dependency.

The lightweight profile type surface is partially ahead of the freeze line: `ProfileId` and `ConsentManifest` are already public in `ferros-profile`, but that does not close G2 by itself because the broader profile fixture, freeze, and CLI evidence is still incomplete.

The signed `CapabilityGrant` envelope path is now landed in `ferros-profile`: Ed25519 sign and verify are implemented, the stripped JSON payload contract is explicitly frozen in `schemas/capability-grant.v0.json`, valid plus invalid grant fixtures are covered by `cargo test -p ferros-profile`, and the H1 contract validator now embeds `schemas/capability-grant.v0.json`, validates `grant-valid.json` against it, and classifies invalid signature rejection as semantic rather than schema-level.

The current pre-G2 convergence path already exercises the real current `CapabilityGrant` type through `ferros-node demo` and the local `ferros agent ...` CLI. Profile-side evidence is no longer limited to fixture parsing: `ferros-profile` now owns fresh Stage 0 profile creation plus create-without-overwrite filesystem persistence, the dedicated `schemas/fixtures/profile-valid.json` golden fixture is proven against the current `schemas/profile.v0.json` freeze candidate in both `ferros-profile` and H1, and `ferros-node` wires a minimal `ferros profile init [path]` / `ferros profile show [path]` slice on top of that boundary with focused cargo-test coverage.

Key material and full profile signing evidence are now in the crate as well: `KeyPair` generates Ed25519 key material with a required local device label, derives `ProfileId` from the verifying key, and signs the additive `SignedProfileDocument` envelope so `cargo test -p ferros-profile` now proves create → serialize → sign → verify → revoke without mutating `schemas/profile.v0.json`. G2 still remains open because `schemas/profile.v0.json` is not yet frozen and the remaining `ferros profile export | import | grant | revoke` CLI evidence is still outstanding.
