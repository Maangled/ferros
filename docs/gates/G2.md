# Gate G2 — Profile v0 Frozen

**Status:** 🟡 Active  
**Stream:** S2  
**Unlocks:** S3 active development; S7 pairing design

---

## Entry criteria

- G1 is closed (CI is green; Cargo workspace exists).
- S2 implementation work is underway.

---

## Required evidence

The following must all be true and verifiable before G2 closes:

- [x] `crates/ferros-profile/` builds and passes `cargo test`.
- [ ] `ProfileId` type is defined and public.
- [x] `CapabilityGrant` type is defined, signed, and verifiable.
- [ ] `ConsentManifest` type is defined.
- [ ] Profile round-trip works end-to-end: create → serialize → sign → verify → revoke.
- [ ] `schemas/profile.v0.json` JSON Schema is present and frozen (no further mutations).
- [x] `schemas/capability-grant.v0.json` JSON Schema is present and frozen.
- [ ] At least one golden fixture in `schemas/fixtures/profile-valid.json`.
- [x] At least one golden fixture in `schemas/fixtures/grant-valid.json`.
- [x] At least one negative fixture: invalid signature is rejected.
- [ ] CLI: `ferros profile init | show | export | import | grant | revoke` all functional.
- [ ] `ferros profile init` followed by `ferros profile show` succeeds on Linux.

---

## Exit criteria

All required evidence items checked. A PR with these items reviewed and merged. Schema version `profile.v0.json` and `capability-grant.v0.json` marked frozen — no further in-place mutations; new fields require a `v1` schema.

---

## Blocking streams / issues

G2 must close before:

| Stream | Blocked item |
|--------|-------------|
| S3 Agent Center | `ProfileId` and `CapabilityGrant` types required for agent manifest and consent check |
| S7 Hub | Pairing flow uses the profile API; cannot design pairing without stable types |

---

## Notes

G2 is now the active gate. G1 is closed, so the remaining work here is implementation and freeze evidence rather than an upstream dependency.

The signed `CapabilityGrant` envelope path is now landed in `ferros-profile`: Ed25519 sign and verify are implemented, the stripped JSON payload contract is explicitly frozen in `schemas/capability-grant.v0.json`, and valid plus invalid grant fixtures are covered by `cargo test -p ferros-profile`.

The current pre-G2 convergence path already exercises the real current `CapabilityGrant` type through `ferros-node demo` and the local `ferros agent ...` CLI. G2 remains open because the broader profile freeze is not finished yet: the full profile-level round-trip and signing story, `schemas/profile.v0.json` freeze, and the `ferros profile init | show | export | import | grant | revoke` CLI evidence listed above are still open.
