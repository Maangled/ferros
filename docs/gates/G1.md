# Gate G1 — Foundation

**Status:** ✅ Closed  
**Stream:** S1  
**Unlocks:** S2–S8 in earnest

---

## Entry criteria

- S1 work is underway (Cargo workspace, CI, site structure).

---

## Required evidence

The following must all be true and verifiable in CI before G1 closes:

- [x] `Cargo.toml` workspace root exists with `[workspace]` members list.
- [x] `rust-toolchain.toml` pins a stable Rust edition.
- [x] `cargo build` exits 0 on `ubuntu-latest`, `macos-latest`, `windows-latest` in CI. Proven by CI run #24812246339 on 2026-04-23.
- [x] `cargo test` exits 0 on all three platforms (empty test suite is acceptable). Proven by CI run #24812246339 on 2026-04-23.
- [x] `cargo fmt --check` exits 0. Proven by CI run #24812246339 on 2026-04-23.
- [x] `cargo clippy -- -D warnings` exits 0. Proven by CI run #24812246339 on 2026-04-23.
- [x] GitHub Actions CI workflow is active and runs on every PR (`ci.yml`). Proven by CI run #24812246339 on 2026-04-23.
- [x] `ferros-blueprint.html` is accessible as `site/index.html`.
- [x] `README.md` reflects actual project state (no aspirational progress bars).
- [x] `CODEOWNERS` exists with at least per-stream path ownership stubs.

---

## Exit criteria

All required evidence items checked. CI is green on a PR that adds a trivial test (e.g., `assert_eq!(1 + 1, 2)`). PR reviewed and merged.

---

## Blocking streams / issues

G1 must close before any of the following are meaningfully started:

| Stream | Blocked item |
|--------|-------------|
| S2 Profile | Meaningful implementation depends on CI-proven foundation and G1 closure |
| S3 Agent Center | Meaningful implementation depends on CI-proven foundation and G2 |
| S4 Runtime | Meaningful implementation depends on CI-proven foundation and G1 closure |
| S5 UX Phase A | Site cleanup depends on `/site/` structure and G1 closure |
| S6 Harvest | Crate extraction depends on CI-proven workspace conventions |
| S7 Hub | Crate implementation depends on G3 and a stable workspace baseline |
| S8 Docs | CONTRIBUTING.md contributor routing assumes CI exists |

---

## Notes

Do not wait for polish on S1. Once the required evidence items are green, G1 closes. Remaining S1 work (xtask, tooling scripts, release workflow) continues in parallel with S2–S8.

Current posture: **G1 is closed.** All required evidence items are checked. CI run #24812246339 (2026-04-23) proved build, test, fmt, and clippy green on ubuntu-latest, macos-latest, and windows-latest. The trivial smoke test in `crates/ferros-core/tests/g1_trivial.rs` served as the G1 evidence artifact.

Manifest note: follow-up audit confirmed there is no unresolved local crate-wiring issue. `xtask` is intentionally std-only, and `ferros-profile` depends on `ferros-core`, `serde`, and `serde_json` as expected.

Closeout: CI matrix proof landed on 2026-04-23 with run #24812246339. Tag `v0.0.1-foundation` and closure of issue #62 / #63 complete the sequence.
