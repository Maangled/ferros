# Gate G1 — Foundation

**Status:** 🟡 In progress  
**Stream:** S1  
**Unlocks:** S2–S8 in earnest

---

## Entry criteria

- S1 work is underway (Cargo workspace, CI, site structure).

---

## Required evidence

The following must all be true and verifiable in CI before G1 closes:

- [x] `Cargo.toml` workspace root exists with `[workspace]` members list.
- [x] `rust-toolchain.toml` pins a stable Rust edition.
- [ ] `cargo build` exits 0 on `ubuntu-latest`, `macos-latest`, `windows-latest` in CI. Local Windows run passed; CI evidence pending.
- [ ] `cargo test` exits 0 on all three platforms (empty test suite is acceptable). Local Windows run passed; CI evidence pending.
- [ ] `cargo fmt --check` exits 0. Local run passed; first CI proof pending.
- [ ] `cargo clippy -- -D warnings` exits 0. Local run passed; first CI proof pending.
- [ ] GitHub Actions CI workflow is active and runs on every PR (`ci.yml`). Workflow file exists; first PR run pending.
- [x] `ferros-blueprint.html` is accessible as `site/index.html`.
- [x] `README.md` reflects actual project state (no aspirational progress bars).
- [x] `CODEOWNERS` exists with at least per-stream path ownership stubs.

---

## Exit criteria

All required evidence items checked. CI is green on a PR that adds a trivial test (e.g., `assert_eq!(1 + 1, 2)`). PR reviewed and merged.

---

## Blocking streams / issues

G1 must close before any of the following are meaningfully started:

| Stream | Blocked item |
|--------|-------------|
| S2 Profile | Crate creation depends on Cargo workspace |
| S3 Agent Center | Crate creation depends on Cargo workspace |
| S4 Runtime | Crate creation depends on Cargo workspace |
| S5 UX Phase A | Site cleanup depends on `/site/` structure |
| S6 Harvest | Crate extraction depends on Cargo workspace |
| S7 Hub | Crate creation depends on Cargo workspace |
| S8 Docs | CONTRIBUTING.md contributor routing assumes CI exists |

---

## Notes

Do not wait for polish on S1. Once the required evidence items are green, G1 closes. Remaining S1 work (xtask, tooling scripts, release workflow) continues in parallel with S2–S8.

Current posture: the artifact side of G1 is mostly in place. The remaining closure work is verification, not architecture: open a PR, let `ci.yml` prove build/test/fmt/clippy, and then merge the trivial-test evidence branch.
