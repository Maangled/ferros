# X86-SUBCORE-CYCLE-2026-05-06-01 Delta Note

Authority: docs/orchestration/AUTHORITY-MAP.md
Date: 2026-05-06
Track: code
Run profile: subcore-runtime

## Implemented contract or seam changes by lane

- L1 runtime seam advance:
  - Added `LocalRunwayAdapter<E, B>` and `LocalRunwayAdapterError<ExecutorError, BusError>` in `crates/ferros-runtime/src/local_runway.rs`.
  - Adapter composes `LocalRunwayState` transition with executor submit and bus route operations using existing `Executor` and `MessageBus` trait seams.
  - Re-exported adapter seam from `crates/ferros-runtime/src/lib.rs`.
- L2 scaffold contract increment:
  - Added `KernelHandoffContract` and `X86_64_KERNEL_HANDOFF_CONTRACT` in `crates/ferros-x86_64-scaffold/src/lib.rs`.
  - Contract binds architecture, kernel artifact family, handoff checkpoint, first-kernel checkpoint, and explicit scope string.
- L3 host rehearsal hardening:
  - Updated runtime smoke and boundary tests to exercise adapter composition in `crates/ferros-runtime/tests/x86_64_subcore_smoke.rs` and `crates/ferros-runtime/tests/boundaries.rs`.
  - Added scaffold tests for kernel-handoff contract vocabulary and non-claim scope.
- L4 foundation contract check:
  - Executed focused `ferros-core` foundation surface tests to confirm deny-by-default and envelope invariants remained intact.
- L6 queue/run-log integrity:
  - Added one new top entry to `docs/orchestration/WAVE-RUN-LOG.md` without editing historical entries.

## Validation evidence summary

- `cargo test -p ferros-runtime --test x86_64_subcore_smoke`: pass (3 passed, 0 failed)
- `cargo test -p ferros-runtime --test boundaries`: pass (7 passed, 0 failed)
- `cargo test -p ferros-x86_64-scaffold`: pass (5 passed, 0 failed)
- `cargo check -p ferros-x86_64-scaffold --no-default-features`: pass
- `cargo test -p ferros-core foundation_surface_`: pass (4 passed, 0 failed)

## Claims added and claims explicitly not added

- Claims added:
  - Runtime seam now has a bounded adapter layer that composes runway transition, executor queueing, and message-bus routing through existing trait boundaries.
  - Scaffold crate now includes one explicit kernel handoff contract type over current artifact and checkpoint vocabulary.
- Claims explicitly not added:
  - No bootloader success claim.
  - No kernel boot success claim.
  - No QEMU boot proof claim.
  - No hardware bring-up claim.
  - No gate closure claim.
  - No FERROS-native runtime claim.

## No-op rationale for optional lanes

- L5 doc-batch truth sync note under `docs/orchestration/doc-batches/` was not required for this single bounded micro-cycle; this delta note under `docs/surfaces/` is the requested serialized truth-sync artifact.
- L7 residual risk closure did not require additional queue mutation because no new blockers emerged during post-flight validation.

## Residual risks and unresolved boundaries

- Adapter error variants are present but only success-path composition is rehearsed; dedicated malformed-route error rehearsal remains open.
- Kernel handoff contract is architecture-contract-only vocabulary and still does not prove boot execution, kernel boot execution, QEMU proof, or hardware proof.
- Non-std runtime portability remains bounded to seam readiness and compile surfaces, not execution proof.

## Next queue seeds

- Seed A: add one focused runtime adapter error-path rehearsal (transition error propagation and bus/executor error mapping) without widening claims.
- Seed B: add one additional scaffold contract for boot artifact provenance that remains architecture-only.
- Seed C: keep truth surfaces synchronized after the next bounded subcore micro-cycle with the same non-claim ledger.
