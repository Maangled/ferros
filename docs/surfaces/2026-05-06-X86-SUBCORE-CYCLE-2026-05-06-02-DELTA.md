# X86-SUBCORE-CYCLE-2026-05-06-02 Delta Note

Authority: docs/orchestration/AUTHORITY-MAP.md
Date: 2026-05-06
Track: code
Run profile: subcore-runtime
Preflight note: active stream session authority-version mismatch remained non-blocking for this cycle and is reported inline here for continuity.

## Implemented contract or seam changes by lane

- L1 runtime error-path rehearsal:
  - Added focused `LocalRunwayAdapter` error-path tests in `crates/ferros-runtime/tests/boundaries.rs`.
  - Rehearsal now covers transition failure with no submit or route side effects, executor failure after state advance, and bus failure after executor submission.
- L2 runtime seam minimal support:
  - No runtime source change was required.
  - Existing adapter behavior in `crates/ferros-runtime/src/local_runway.rs` was sufficient to satisfy the new error-path evidence without broadening the seam.
- L3 scaffold provenance increment:
  - Added `BootArtifactLineageContract` and `X86_64_BOOT_ARTIFACT_LINEAGE_CONTRACT` in `crates/ferros-x86_64-scaffold/src/lib.rs`.
  - Contract ties x86_64 boot artifact, kernel artifact, serial observation artifact, and kernel-handoff checkpoint into one architecture-only lineage statement.
- L4 foundation contract guard:
  - Re-ran focused `ferros-core` foundation surface coverage with no code changes.
  - Deny-by-default behavior and envelope token invariants remained intact.
- L5 truth-sync settlement:
  - Added this delta note under `docs/surfaces/`.
  - Appended one newest-first run outcome to `docs/orchestration/WAVE-RUN-LOG.md` without editing historical entries.

## Changed files mapped to lane objective

- `crates/ferros-runtime/tests/boundaries.rs`: L1 runtime error-path rehearsal.
- `crates/ferros-x86_64-scaffold/src/lib.rs`: L3 scaffold provenance increment.
- `docs/surfaces/2026-05-06-X86-SUBCORE-CYCLE-2026-05-06-02-DELTA.md`: L5 serialized truth-sync artifact.
- `docs/orchestration/WAVE-RUN-LOG.md`: L5 append-only run settlement.

## Validation evidence summary

- `cargo test -p ferros-runtime --test boundaries`: pass (10 passed, 0 failed)
- `cargo test -p ferros-runtime --test x86_64_subcore_smoke`: pass (3 passed, 0 failed)
- `cargo test -p ferros-x86_64-scaffold`: pass (7 passed, 0 failed)
- `cargo check -p ferros-x86_64-scaffold --no-default-features`: pass
- `cargo test -p ferros-core foundation_surface_`: pass (4 passed, 0 failed)

## Claims added and claims explicitly not added

- Claims added:
  - Runtime boundary rehearsal now explicitly covers `LocalRunwayAdapter` transition, executor, and bus error mapping through the existing host-side seam.
  - Scaffold vocabulary now includes one architecture-only boot artifact lineage contract tied to current artifact families and kernel-handoff checkpoint vocabulary.
- Claims explicitly not added:
  - No bootloader success claim.
  - No kernel boot success claim.
  - No QEMU boot proof claim.
  - No hardware bring-up proof claim.
  - No gate closure claim.
  - No FERROS-native runtime proof claim.

## Residual gaps reduced and residual gaps still pre-native

- Reduced in this cycle:
  - The prior open adapter error-path rehearsal gap is now closed for transition mapping and executor or bus propagation in the hosted boundary harness.
  - Scaffold provenance vocabulary now covers one explicit artifact-lineage contract instead of stopping at kernel handoff vocabulary only.
- Still pre-native:
  - These rehearsals remain host-side seam evidence, not native runtime execution evidence.
  - The adapter is not claimed to be atomic or rollback-safe; the bus-failure rehearsal intentionally shows state advance and queued executor work can persist after route failure.
  - No boot execution, kernel execution, QEMU execution, or hardware execution proof was added.
  - No new stream contract index or gate surface was advanced.

## No-op rationale for optional lanes

- No runtime source refactor was performed because the existing adapter seam already satisfied the new failure-path evidence.
- No additional `ferros-core` source change was required because the foundation guard passed unchanged.
- No queue mutation was required beyond the append-only run-log update and this serialized delta note.

## Next queue seeds

- Seed A: decide whether adapter post-failure semantics should be documented explicitly so downstream readers do not infer rollback guarantees.
- Seed B: add one additional architecture-only lineage or checkpoint contract only if it stays below execution-proof language.
- Seed C: keep future truth-sync notes explicit about hosted rehearsal versus pre-native gaps when the x86_64 incubation seam moves again.
