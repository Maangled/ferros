[package]
name = "ferros-x86_64-scaffold"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[features]
default = ["std"]
std = ["ferros-core/std"]

[dependencies]
ferros-core = { path = "../ferros-core", default-features = false }

[lib]
path = "src/lib.rs"