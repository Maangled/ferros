[package]
name = "ferros-agents"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
ferros-profile = { path = "../ferros-profile" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

[lib]
path = "src/lib.rs"